kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: skypilot-e2e
nodes:
  - role: control-plane
    extraPortMappings:
      # Expose API server NodePort for sky CLI access
      - containerPort: 30050
        hostPort: 30050
        protocol: TCP
